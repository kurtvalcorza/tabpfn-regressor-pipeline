# TabPFN Regression Tutorials

[![GitHub](https://img.shields.io/badge/GitHub-181717?style=flat&logo=github&logoColor=white)](https://github.com/kurtvalcorza/tabpfn-regressor-pipeline)
[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/kurtvalcorza/tabpfn-regressor-pipeline/blob/main/tutorials/tabpfn_regressor_colab.ipynb)
[![Hugging Face](https://img.shields.io/badge/%F0%9F%A4%97%20Hugging%20Face-Prior--Labs%2Ftabpfn__3-ffcc4d?style=flat)](https://huggingface.co/Prior-Labs/tabpfn_3)
[![Upstream](https://img.shields.io/badge/Upstream-PriorLabs%2FTabPFN-181717?style=flat&logo=github&logoColor=white)](https://github.com/PriorLabs/TabPFN)
[![arXiv](https://img.shields.io/badge/arXiv-2605.13986-b31b1b.svg)](https://arxiv.org/abs/2605.13986)

Notebook specification: **DIMER Notebook Specification 2.0** — both notebooks are **standalone** (§4): they are
generated by `tools/build_notebook.py` from `tools/notebook_template.py` (task inference) and
`tools/notebook_template_artifact_inference.py` (artifact inference) and carry the repository's pipeline module,
model identity, snapshot manifest and runtime pins, so the exported `.ipynb` keeps working without this repository
being reachable. Do not edit the notebooks by hand; edit the package or the templates and regenerate
(`python tools/build_notebook.py`, `python tools/build_notebook.py --template tools/notebook_template_artifact_inference.py`;
`--check` is enforced by the validator and CI).

**Profile change and dropped path.** The previous notebook pair (NOTEBOOK_SPEC 1.0) cloned this repository and the
private `tabpfn-regressor-finetuner` worker with a `GITHUB_TOKEN` and ran `train.run()` (`E2E`). That path is dropped:
the worker is private and the TabPFN-3 weights are non-commercial, so the standalone tutorial is **inference-only**
(`TASK-INFERENCE`: in-context conditioning on the pinned checkpoint, no gradient update) and says so in its own prose.
The companion keeps the `ARTIFACT-INFERENCE` profile on the same `model.tabpfn_fit` + `model.ckpt` +
`artifact_manifest.json` bundle contract the worker and the serving loader use.

| Notebook | Profile | Mode | Carrier | Capability | Default runtime | Sample | BYOD | Run-all | Release status |
|---|---|---|---|---|---|---|---|---|---|
| `tabpfn_regressor_colab.ipynb` | `TASK-INFERENCE` | `GUIDED` | standalone (generated) | Synthetic or BYOD table → `validate_inputs` (input manifest with a recorded rejection) → in-context fit on the pinned `tabpfn-v3-regressor-v3_default.ckpt` → `mae`, `rmse`, `r2`, `mape` (non-zero targets only) vs the training-mean baseline (`mean_baseline`) → `evaluation_report` (`sample-sanity`) → artifact bundle → fresh-boundary reload (recomputed validation MAE equal to the recorded value within `1e-6`) → new-row scoring → exports | CPU (CUDA used automatically when available) | automatic | one train/val/test ZIP (`USE_BYOD`) and one CSV of new rows (`USE_BYOD_ROWS`), both gated off by default | verified — clean-runtime `Run all` execution recorded (REL1/REL8) | **Candidate** — static checks pass; the clean-runtime execution run is clean-room execution recorded in `../docs/release-verification.md` |
| `tabpfn_regressor_artifact_inference_colab.ipynb` | `ARTIFACT-INFERENCE` | `GUIDED` | standalone (generated) | Externally produced bundle ZIP → `safe_extract_zip` → `validate_artifact_bundle` before any deserialisation (bundled `model.ckpt` must equal the pinned checkpoint; optional expected digests) → `from_artifact` → `validate_new_rows` → predict → exports; the evaluation report is `not-measurable` (no labels) | CPU (CUDA used automatically when available) | **none yet** — default path uploads both the bundle and the rows (§19 SART1/RUN5 gap) | bundle ZIP + new-rows CSV by upload or explicit path (no sample is bundled) | verified — clean-runtime `Run all` execution recorded (REL1/REL8) | **Candidate** — static checks pass; external-artifact clean-runtime evidence pending in `../docs/release-verification.md` |

## Conformance notes

_Requirement identifiers in these notes (for example `ST1`, `PAR3`, `DAT24`, `EVAL21`) are the NOTEBOOK_SPEC 1.1 numbering they were written against; NOTEBOOK_SPEC 2.0 renumbered its sections and the pull request that migrated this repository reports conformance against the 2.0 pre-flight checklist (§26) and report format (§30)._

- **Standalone carrier (ST1–ST6):** the primary path performs no clone, repository install, repository import, worker call or subprocess, and reads no token. Section 2 is `src/tabpfn_regressor_pipeline/pipeline.py` verbatim (tagged `metadata.dimer.embedded_module`; the only rewrite makes the default weights directory working-directory-relative), Section 3 carries the model identity and the 4-file SHA-256 manifest inline and asserts they agree with the module before anything is fetched, and Section 1 carries the exact `pyproject.toml` runtime pins (`torch==2.11.0`, `torchvision==0.26.0`, `tabpfn==8.1.0`, `pandas==2.3.2`, `scikit-learn==1.9.0`, `huggingface-hub==0.36.2`). `metadata.dimer.generated_from` records the repository, the module's last commit, its SHA-256, and the generator version.
- **Parity (PAR1–PAR4):** `tests/test_notebook_parity.py`, `tests/test_companion_parity.py` and `tools/validate_release_assets.py` fail when the carried module, the inline manifest, or the inline pins differ from the repository, or when a notebook is not byte-identical to the generator's output.
- **Model acquisition:** `stage_missing_files(WEIGHTS_DIR, allow_download=True)` fetches only the manifest entries that are absent (`tabpfn-v3-regressor-v3_default.ckpt`, `config.json`, `LICENSE`, `README.md`) from the Hugging Face Hub at the immutable revision `24a16a89d245878b846555110985634aa2e656d7` (never `main`; `Prior-Labs/tabpfn_3` is public and not access-gated); `verify_snapshot` re-hashes every entry and additionally requires the checkpoint's digest to equal the module constant `WEIGHTS_SHA256`; `from_pretrained(weights_dir=WEIGHTS_DIR)` binds the verified file as `model_path` for `tabpfn.TabPFNRegress`.
- **Adaptation:** in-context conditioning only — `fit` registers the training rows as the support set and performs no gradient update; the notebook and the evaluation report (`adaptation`) both state that the private-worker fine-tune path is not carried. Seeds are explicit (`SAMPLE_SEED`, `SEED`); `N_ESTIMATORS` is a form field.
- **Role stages:** `validate_inputs` (DAT24) writes `outputs/tabpfn_regressor_input_manifest.json` with the schema, the v3 ceilings (`MAX_TRAIN_ROWS` 1,000,000, `MAX_FEATURES` 2,000, `MIN_TRAIN_ROWS` 10), the feature columns, per-split counts, a digest of the training table, warnings as non-fatal findings and one recorded rejection from a renamed-target probe; `evaluation_report` (EVAL21) writes `outputs/tabpfn_regressor_evaluation_report.json` — `sample-sanity` on the single holdout with the baseline and the reload check; `not-measurable` when no labelled split was scored (always the case in the companion).
- **Artifact contract and trust boundary:** `save_artifact` writes `model.tabpfn_fit` (fitted state **including the in-context training rows**) + `model.ckpt` (byte copy of the verified checkpoint) + `artifact_manifest.json` (task, target, ordered features, per-file SHA-256, base-model identity); `validate_artifact_bundle` checks schema, member names, sizes, digests and archive safety **before** deserialisation, and both notebooks refuse a bundle whose checkpoint is not the pinned one. Digest checks establish internal consistency, not sender authenticity: `model.tabpfn_fit` / `model.ckpt` are serialised Python/torch state and loading them executes trusted model state. The bundle contains training rows — apply the dataset's confidentiality controls to it.
- Regression obligations: the prediction is TabPFN's point estimate (mean of the predictive distribution); no prediction intervals are shipped; exported predictions carry `prediction` as a continuous point estimate. The default sample is a deterministic synthetic table drawn in code (`build_synthetic_dataset`); its metrics are plumbing evidence only.
- **BYOD:** one ZIP laid out as `train.csv` (+ optional `val.csv`, `test.csv`) read member by member by `read_dataset_zip` (bare file names only, expanded-size and compression-ratio ceilings; never `extractall`); without `val.csv` a seeded stratified holdout is drawn (independent rows assumed). Gates default to `False` so the sample path never opens an upload dialog; executors supply `BYOD_ZIP_PATH` / `NEW_DATA_PATH` / `ARTIFACT_ZIP_PATH` instead.
- `tools/validate_release_assets.py` performs source validation only (`scripts/validate_colab_tutorial.py` delegates to it). It does not satisfy the clean-runtime execution requirement; a release review must confirm that a recorded clean run in `docs/release-verification.md` matches the notebook revision under review before the status is promoted to `Release-grade`. `scripts/execute_notebook_release.py` is the previous pair's bootstrap-stripping harness and does not apply to the standalone notebooks (they have no bootstrap region); run them directly with a kernel executor.
