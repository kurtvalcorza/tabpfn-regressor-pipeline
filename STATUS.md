# Release status

Current status: **Candidate — tutorial source complete, awaiting clean-runtime execution**. The pipeline package `src/tabpfn_regressor_pipeline/` (pinned TabPFN-3 snapshot scheme, input contract, in-context fit / predict / evaluate, the `model.tabpfn_fit` + `model.ckpt` + `artifact_manifest.json` bundle with its pre-load validation, and the evaluation report), the offline unit tests, `MODEL_CARD.md`, and the two standalone tutorials `tutorials/tabpfn_regressor_colab.ipynb` (`TASK-INFERENCE`) and `tutorials/tabpfn_regressor_artifact_inference_colab.ipynb` (`ARTIFACT-INFERENCE`) — generated by `tools/build_notebook.py`, DIMER Notebook Specification 1.1 §3.6 — exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks). The previous notebook pair drove the private DIMER fine-tuner worker; that path is dropped from the standalone notebooks (private code, non-commercial TabPFN-3 weights) and their execution records do not carry over. The repository is not release-grade until clean-runtime execution evidence for the exact candidate commit is recorded in `docs/release-verification.md`.
